# 信号采集程序 v3：参数、频谱与性能说明

## 1. 本版解决的问题

- 界面中的中心频率、采样率、带宽、增益、通道和天线现在会真正配置到 UHD；
- FFT 支持 1024、2048、4096、8192、16384 点；
- 支持 Hann、Hamming、Blackman-Harris、Flat Top 四种窗函数；
- Average 和 Max Hold 只在相应复选框勾选时计算和显示，默认关闭；
- 时域图采用自动量程，弱信号不再因为固定 ±1 纵轴而看起来像一条空线；
- 状态栏右下角显示本进程 CPU 占用率和实际显示帧率；
- 定长采集和 IQ 保存仍明确禁用，当前只实现连续采集。

## 2. 参数如何进入接收线程

### 2.1 设备状态与安全的接口识别

设备未连接时，型号显示“未知”。连接成功后，设备层返回并显示：

- UHD 报告的实际设备型号；
- 当前连接方式（现阶段为 UHD）；
- 本次实际使用的 device args；
- 接口类型。

接口类型只根据 UHD 已安全返回的设备描述和连接参数识别，不再直接读取
不同设备、不同 UHD 版本之间结构不一致的 property tree。若能确认 USB 代际，
显示 `USB 2.0` 或 `USB 3.0`；只能确认是 USB 时显示 `USB（速率未知）`，
不会猜测。这样可以避免连接成功后因访问不兼容属性路径而异常退出。

`MainWindow::startReceiving()` 从界面读取参数，生成 `RxConfig`：

```text
UI 控件 → RxConfig（只读副本）→ RxWorker → UHD / SpectrumProcessor
```

Worker 构造后配置不再改变。因此采集中参数控件会被禁用；停止后修改，再重新开始即可生效。这比 GUI 和接收线程同时读写同一批参数更安全，也便于复现实验。

这里的“前端带宽”调用 UHD `set_rx_bandwidth()`，控制 USRP 接收前端的
模拟/数字滤波带宽。它不是 FFT 横轴的显示范围。当前频谱显示跨度由采样率
决定，理论范围为 `中心频率 - Fs/2` 到 `中心频率 + Fs/2`。

“前端带宽跟随采样率”勾选时：

```text
analog bandwidth = sample rate
```

取消勾选后使用“前端带宽”输入框的值。现在勾选状态和带宽数值都可在
接收过程中调整，并立即提交给接收线程。日志和右侧状态区显示 UHD 实际接受
的带宽；设备可能根据自身能力将请求值调整到最近的可用值。

连接参数与设备参数是两个概念：

- 连接参数用于选择某一台 USRP，例如 `serial=...`、`type=x300,addr=...`；
- 设备参数用于 UHD 初始化选项，例如 `master_clock_rate=200e6,clock_source=external`。

两栏最终按 UHD device args 语法合并后创建设备，但查找设备时只使用连接参数，
这样多机环境中不会混淆“选哪台设备”和“如何初始化设备”。

顶部“截图保存”会截取当前频谱页，包括控制栏、频谱、瀑布和时域图，并保存
为 PNG。默认文件名带时间戳，保存成功或失败都会给出明确反馈。

参考电平只改变频谱图纵轴显示范围，不改变 USRP 的射频增益。当前纵轴仍是相对 dBFS，尚未经过 dBm 校准。

## 3. 为什么以前选择大 FFT 仍可能不生效

`rx_streamer->recv()` 一次返回的样本数由 UHD/USB 传输包决定，可能少于 4096。旧代码只处理单个接收包，所以实际 FFT 点数会被接收包大小限制。

本版使用两个暂存数组跨包收集 I/Q：

```text
recv 包 1 ┐
recv 包 2 ├→ 凑齐用户选择的 N 点 → 窗函数 → N 点 FFT
recv 包 3 ┘
```

因此 8192 和 16384 点现在确实会按所选点数计算。FFT 点数越大，频率间隔越小：

```text
bin spacing = sample rate / FFT size
```

例如 5 MS/s、4096 点时约为 1220.7 Hz/bin；16384 点时约为 305.2 Hz/bin。这里是频点间隔，不等同于窗函数的等效噪声带宽。

## 4. 四种窗函数适合什么测试

| 窗函数 | 特点 | 建议用途 |
| --- | --- | --- |
| Hann | 综合折中，泄漏较低 | 日常频谱观察，默认选择 |
| Hamming | 第一旁瓣较低 | 一般窄带信号观察 |
| Blackman-Harris | 旁瓣抑制很强、主瓣较宽 | 强信号旁边找弱信号 |
| Flat Top | 幅度准确性较好、主瓣最宽 | 单音幅度测量 |

## 5. Average 与 Max Hold

Average 在功率域做指数平均：

```text
alpha = 2 / (N + 1)
Pavg = alpha × Pcurrent + (1 - alpha) × Pavg
```

`N` 是界面的平均次数。N 越大，曲线越稳定，但响应变化越慢。

Max Hold 对每个 FFT bin 保存本次采集开始以来的最大值：

```text
maxHold[i] = max(maxHold[i], current[i])
```

Average 和 Max Hold 都可以在接收过程中打开或关闭。打开或切换模式时会清空对应历史，防止不同配置的数据混合：

- Current：黄色；
- Average：青蓝色；
- Max Hold：红色。

“实时波形”开关只控制 Current 的显示。关闭它不会停止 FFT 处理，因此可以只观察 Average、Max Hold 或两者组合。

## 6. 时域图为什么现在能看见

IQ 样本的归一化幅度经常远小于 1。旧图固定使用 ±1 范围，较弱的 I 路波形会挤在中心线上。本版每帧测量 I 路峰值并设置自动量程，同时在右上角显示 `AUTO ±...`。

当前只显示 I 分量，目的是先验证采样数据和时序。后续可以增加 I/Q 双 Trace、幅度/相位以及触发功能。

## 7. CPU 与刷新率

状态栏右下角：

- `CPU`：当前程序进程占整机总 CPU 能力的比例；
- `Display FPS`：最近一秒实际送到三个绘图控件的帧数。

采集率与显示帧率是两回事。程序仍持续接收完整 IQ，但约每 20 ms 生成一帧显示数据，目标约 50 FPS。FFT 在接收线程完成，GUI 只绘图；瀑布使用环形图像，每帧只写入一行。

频谱曲线会按绘图区的实际像素宽度压缩。例如 16384 点 FFT 显示在 900 像素宽区域时，不再绘制 16384 段线，而是每个像素列保留该频段最大值。这既避免漏掉窄带峰值，也能明显降低绘制负担。该思路与 GNU Radio QT GUI 一样，将数据处理速率和屏幕绘制需求分开考虑。默认 FFT 点数为 1024，以优先保证实时流畅度；需要更高频率分辨率时再在线切换到更大点数。

## 8. 采集中可调参数

以下参数无需停止接收即可修改：

- 中心频率、采样率、模拟带宽和增益；
- FFT 点数、窗函数、平均次数；
- Current、Average、Max Hold 显示状态；
- 参考电平和瀑布色阶范围。

GUI 不直接调用 UHD。它只把新配置写进一个互斥锁保护的副本，接收线程在下一次循环中读取并调用 `set_rx_*()`。这样避免 GUI 与接收线程同时访问 `multi_usrp`。

FFT 点数或窗函数改变时，程序会清空未完成的 FFT 样本以及 Average/Max Hold 历史。采样率改变且启用“带宽跟随采样率”时，模拟带宽会同步更新。

通道、天线和“带宽跟随”策略仍在采集中锁定，因为它们可能改变流结构；停止后可修改。

## 9. 瀑布显示增强

“瀑布范围”的低值和高值控制 dBFS 到颜色的映射。例如 `-125 ~ -20 dBFS` 是较宽的默认范围；若弱信号集中在 `-100 dBFS` 左右，可改为 `-115 ~ -75 dBFS`，颜色对比会明显增强。它只改变显示，不改变 FFT 数据。

## 10. 绘图库结论

最终第三方显示后端推荐 **Qwt**，理由是它面向 Qt Widgets 技术仪表，具有成熟的曲线、坐标轴、Marker 和 Spectrogram 组件，并且不要求独立显卡。它比 QCustomPlot 更适合频谱仪结构，也比强制 GPU 的商业库更适合核显、远程桌面和驱动情况不统一的客户机。

本包暂未直接链接 Qwt，原因是 Qwt 需要先作为依赖安装或随工程构建；直接切换会让你当前已经能编译的工程立即因缺少 Qwt 而失败。本版先完成算法、参数和性能基线。确认数据正确后，再把 `signal_plot_widgets` 替换为 Qwt 后端，UHD、线程和 `SpectrumProcessor` 都不需要重写。

## 11. 主要文件职责

| 文件 | 职责 |
| --- | --- |
| `rx_config.hpp` | 一次接收任务的参数快照 |
| `rx_receiver.hpp` / `rx_worker.cpp` | UHD 配置、连续接收、跨包组帧和统计 |
| `spectrum_processor.hpp/.cpp` | 窗函数、FFT、Average、Max Hold |
| `signal_plot_widgets.hpp/.cpp` | 当前兼容型 CPU 绘制后端 |
| `main_window.hpp/.cpp` | 读取 UI、线程生命周期、CPU/FPS 状态 |
| `main_window.ui` | 参数和显示布局 |

## 12. v8 新增的仪表控制

- 连接参数已经移动到“设备连接状态”，其作用等价于 UHD `--args`，例如 `serial=...` 或 `type=x300,addr=...`；“设备参数”仍用于 `master_clock_rate`、`clock_source` 等初始化设置。
- Min Hold（最小保持）在接收线程逐频点计算，紫色显示；采集中可以打开或关闭，状态切换时会清空保持历史。
- Marker 数量可选 0～4。M1～M4 按当前实时频谱的局部峰值由强到弱自动放置，并显示频率与补偿后的 dBFS。
- 输入补偿单位为 dB。例如信号经过 6 dB 衰减器/线缆，可输入 `+6 dB`。补偿应用于 Current、Average、Max Hold、Min Hold、Marker 和瀑布；它不会改变 UHD 硬件增益，也不作用于原始 IQ 数据。
- 左侧顶部会读取 exe 同级 `assets/company_logo.png`。建议透明 PNG、约 3:1、600×200 px；找不到文件时保留占位文字。可直接替换该文件并重启，无需重新编译。

## 13. 建议测试顺序

1. 全新构建并连接 B210；
2. 先用 100 MHz、5 MS/s、5 MHz、20 dB、4096 点 Hann；
3. 确认时域图出现黄色 I 路波形；
4. 接收中依次开启 Average 和 Max Hold，确认青蓝/红色曲线出现；
5. 关闭“实时波形”，确认黄线消失但 Average/Max Hold 保留；
6. 接收中修改增益和采样率，确认实际参数与频谱跨度更新；
7. 接收中测试四种窗函数与 1024～16384 点 FFT；
8. 缩小瀑布低/高范围，确认弱信号颜色对比增强；
9. 记录各点数下 CPU、FPS、窗口缩放是否流畅；
10. 对照已知信号源频率，确认 Peak 频率是否正确。
11. 依次启用 1～4 个 Marker，确认标记不会全部落在同一主瓣；输入 `+6 dB` 后确认所有频谱读数整体上移约 6 dB。
12. 开启 Min Hold，确认紫色最低保持线出现；关闭再开启后确认历史重新开始累计。

目前频谱幅度没有设备级校准，不能用它验证绝对 dBm；可以先验证频率、相对幅度、窗函数、FFT 点数和时间变化是否正确。
# v9：顶部布局与可控 Marker

- 顶部频谱控制区按统一标签宽度、统一控件高度重新对齐，并将 Marker 独立放在第三行，避免窗口缩窄时与窗函数、保持功能重叠。
- Marker 从“每帧自动寻找前四个峰值”改为四个独立、有状态的测量标记 M1～M4。
- 选择 M1～M4 后，可通过频率输入框精确定位，也可直接单击频谱图定位。
- “峰值”将当前 Marker 定位到全局最高局部峰；“下一峰”按峰值强度循环选择彼此分离的局部峰。
- 默认情况下 Marker 保持绝对频率，不随每帧噪声变化。
- 开启“峰值跟踪”后，只在当前 Marker 附近约 2% FFT 点数的范围内寻找最高点，避免跳到频谱另一端。
- 中心频率、采样率或 FFT 点数改变时，Marker 继续保持原频率；超出当前显示跨度时暂时不绘制。
